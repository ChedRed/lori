name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test-cli:

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        args:
        - "-t ./tests/main.lua"

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Build Lori
      run: cargo build --release

    - name: Test Lori
      run: |
        set -e
          ./target/release/lori ${{ matrix.args }}
